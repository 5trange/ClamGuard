[project]
name = "clamguard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyside6>=6.11.1",
    "pywin32>=312 ; sys_platform == 'win32'",
    "watchdog>=6.0.0",
]
